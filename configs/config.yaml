defaults:
  - data: synthetic
  - model: default
  - detector: detector
  - vlm: vlm
  - train: default
  - _self_

# Global settings
seed: ${train.seed} # Use the seed from the training config
device: cuda
precision: 16

# Logging configuration
logging:
  level: INFO
  jsonl: true
  dir: experiments/${now:%Y-%m-%d}/${now:%H-%M-%S}
